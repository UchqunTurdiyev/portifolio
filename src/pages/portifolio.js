import { Portifolio } from '@/components';
import { withLayout } from '@/layout/layout';
import React from 'react';

const PortifolioPage = () => {
	return <Portifolio />;
};

export default withLayout(PortifolioPage);
